# 数值压缩包合并说明

已合并到 `/Users/ywh/Documents/数值`。来源：`/Users/ywh/Downloads/数值.rar`。

## 恢复与合并

压缩包工作目录仅有9个与本地相同的评审文件，另含未完成的 rebase 元数据。通过只读 Git 对象恢复 `2a477b8`（Update battle page）保存的37个文件，去掉多余的“数值/”目录层级后合并。没有运行包内 rebase 指令、hooks 或覆盖当前 `.git`。

- 新增 `玩法.docx`、`网页版/battle.html`、`battle-v2.js`、`battle.css`；旧 `battle.js` 归档到 `网页版/历史版本/战斗初版/`。
- 根目录 `新数值.xlsx` 采用包内更新版本，共 318 个单元格值有变化（包括新增、清空和文字修改）；25个灵兽、75个技能，共100个基础物品。逐格记录见 `合并记录/数值表逐格差异.json`。
- 当前图谱 v0.2、小循环示例、搜索、关系证据和单文件功能保留；更新8处失效关系片段及玄铁龟充能示例，重新生成数据和附件。未用包内旧图谱覆盖当前版本。
- 战斗页接入当前 `ATLAS_DATA`，恢复包内丢失的配置样式，并修正6列5行棋盘及手机布局。
- 原始 PDF、评审文件和三场历史战斗日志保留。评审脚本和旧模拟分别固定读取目录内的 `source` 快照，防止旧结果与新数值混用。
- 忽略 `.git`、`__MACOSX`、`.DS_Store` 等仓库及系统元数据，未把包内缺失文件视为本地删除指令。

## 验证范围

当前图谱逐格来源、54条关系片段、附件哈希及离线单文件交互通过检查；三套小循环示例检查通过；历史模拟7项测试通过。战斗页验证结果见 `网页版/verification/battle-results.json`。

战斗页为包内已有原型，含硬编码效果、品质倍率及暂定规则。本次完成内容合并与页面兼容性验证，不代表逐条实现或验收 `玩法.docx` 的全部游戏规则。旧评审和三场日志仍仅说明旧基线。

## 入口与备份

- 机制图谱：`网页版/index.html`
- 战斗沙盒：`网页版/battle.html`
- 离线图谱：`网页版/交互关系_单文件分享版.html`
- 合并前完整文件备份：`/Users/ywh/Downloads/数值_合并前备份_20260909.zip`

本次未提交、推送或部署。
